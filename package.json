{
  "name": "trailblazers-dev-platform",
  "description": "Dev Platform for trailblazer team",	
  "keywords": [
	"Family tree mockup",
	"d3 data visualization"
	],
  "directories": {
    "test": "test",
    "src": "src"
  },
  "author" : "Trailblazer",
  "repository": {
	"type": "git",
	"url": "https://github.com/younited/trailblazers.git"
	},
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
	  "prestart": "npm install",
    "start": "http-server app -a localhost -p 8000 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
